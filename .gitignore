node_modules
.next
out
next-env.d.ts
*.tsbuildinfo

# Secretos: nunca commitear .env con valores reales (.env.example sí se versiona)
.env
.env.*
!.env.example

# Almacenamiento privado local (comprobantes, etc. en dev)
.private-uploads

# Configuración local del editor/Claude Code (no es del proyecto)
.claude/

# Datos personales reales para la importación del Bloque 3: nunca se commitea.
/importacion-temporal/
