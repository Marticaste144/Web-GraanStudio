import { ClasesAdmin } from "@/components/admin/ClasesAdmin";

export const metadata = { title: "Clases · Administración Graan Studio" };

export default function AdminClases() {
  return <ClasesAdmin />;
}
