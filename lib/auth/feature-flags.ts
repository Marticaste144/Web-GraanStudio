// Interruptores temporales y explícitos, para no dispersar "if" mágicos por el código.

/**
 * Alta pública de alumnas (/registro): deshabilitada hasta que definamos el flujo real de
 * onboarding (WhatsApp + Admin). Volver a poner en `true` cuando ese flujo esté decidido,
 * o reemplazar este chequeo por la lógica definitiva en ese momento.
 */
export const REGISTRO_PUBLICO_HABILITADO = false;
